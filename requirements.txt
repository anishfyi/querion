# Core
pyyaml>=6.0
psycopg2-binary>=2.9
requests>=2.28

# Web UI + API (querion serve)
fastapi>=0.110
uvicorn>=0.27

# Charts (image rendering; a unicode fallback works without it)
matplotlib>=3.7

# Loads a .env file automatically for the CLI
python-dotenv>=1.0
